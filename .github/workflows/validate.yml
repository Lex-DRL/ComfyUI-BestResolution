name: 🕵️️‍♂️Validate backwards compatibility

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
    paths:
      - "**.py"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: comfy-org/node-diff@main
